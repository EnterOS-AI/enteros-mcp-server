# repo-meta.yaml — SSOT manifest for the meta-CI capability→bundle router (RFC #57).
# Declares WHAT this repo IS (layer) and WHAT IT CAN DO (capabilities); the router
# derives which CI bundles attach. See molecule-ci/docs/meta-ci.md.
# ADVISORY (RFC #57 Phase 2 — deferred Node/TS repos) — non-blocking; no branch-protection change.
schema_version: 1
layer: service
capabilities:
  - node-package
